module.exports = {
    values:{
        error:{
            noMocker: "Cannot call this function without first creating a mocker, use mocker.mock() to create a new mocker",
            noArgs: "No [Args] object passed to [Function] function",
            noMatchingArgs: "No matching aruments found, cannot delete"
        }
    }
};